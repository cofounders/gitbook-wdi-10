<% if @mood.errors.any? %>
	<ol>
		<% @mood.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
	</ol>
<% end %>

<%= form_for @mood do |f| %>
	<p>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</p>

	<%= f.submit %>
<% end %>